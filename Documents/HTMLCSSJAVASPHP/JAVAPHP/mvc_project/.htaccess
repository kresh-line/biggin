# Aktivizo rewrite engine
RewriteEngine On

# Baza e projektit
RewriteBase /mvc_project/

# Nëse nuk është fajll i vërtetë dhe nuk është direktori,
# ridrejto tek index.php?url=...
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^(.+)$ index.php?url=$1 [QSA,L]
